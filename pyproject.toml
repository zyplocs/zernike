[project]
name = "vision"
version = "0.1.0"
description = "Visual-optics explorations: Zernike polynomial atlas and wavefront surfaces."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11,<4",
    "numpy>=2.5,<3",
]

[dependency-groups]
dev = [
    "ipython>=9",
]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["plot_zernike"]
